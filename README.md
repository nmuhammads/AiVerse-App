# AI Image Generator - Telegram Mini App

Telegram Mini App для генерации и редактирования изображений с использованием AI моделей через Kie.ai API.

## Особенности

- 🎨 **Множество AI моделей**: Flux, Seedream 4, Nanobanana (Gemini 2.5 Flash), Qwen Edit
- 📱 **Telegram Mini App**: Полная интеграция с Telegram SDK
- 🔄 **Редактирование изображений**: Поддержка Qwen Edit для редактирования существующих изображений
- 📐 **Гибкие пропорции**: Поддержка различных соотношений сторон (1:1, 16:9, 9:16)
- 💾 **Скачивание и分享**: Возможность скачивания и分享 готовых изображений
- 🌙 **Адаптивный дизайн**: Современный UI с поддержкой темной темы

## Технологический стек

- **Frontend**: React 18, TypeScript, Vite, Tailwind CSS
- **Backend**: Node.js, Express, TypeScript
- **UI компоненты**: shadcn/ui (кастомная реализация)
- **State Management**: Zustand
- **Telegram SDK**: @twa-dev/sdk
- **AI API**: Kie.ai (OpenAI-compatible)

## Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd ai-image-generator-tma
```

### 2. Установка зависимостей

```bash
pnpm install
```

### 3. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните необходимые переменные:

```bash
cp .env.example .env
```

**Необходимые переменные:**
- `KIE_API_KEY` - API ключ от Kie.ai
- `NODE_ENV` - окружение (development/production)
- `PORT` - порт для сервера (по умолчанию 3000)

### 4. Запуск в режиме разработки

```bash
pnpm run dev
```

Приложение будет доступно по адресу `http://localhost:3000`

## Использование

### 1. Выбор модели

Приложение поддерживает 4 модели:
- **Flux** - Базовая модель для генерации изображений из текста
- **Seedream 4** - Высококачественная генерация изображений
- **Nanobanana** - Быстрая генерация на основе Gemini 2.5 Flash
- **Qwen Edit** - Редактирование существующих изображений

### 2. Генерация изображений

1. Выберите модель во вкладках
2. Введите описание (prompt) на английском языке
3. Выберите соотношение сторон (для text-to-image моделей)
4. Нажмите "Generate" или используйте Telegram MainButton

### 3. Редактирование изображений (Qwen Edit)

1. Выберите модель "Qwen Edit"
2. Загрузите изображение для редактирования
3. Опишите, что вы хотите изменить
4. Нажмите "Edit Image"

### 4. Работа с результатами

- **Скачать**: Нажмите кнопку "Download" для сохранения изображения
- **分享**: Нажмите кнопку "Share" для分享 через Telegram
- **Новая генерация**: Нажмите "Generate Another" для возврата к форме

## API Endpoints

### POST /api/generation/generate

Генерация изображения через Kie.ai API

**Тело запроса:**
```json
{
  "prompt": "a beautiful sunset over mountains",
  "model": "flux",
  "aspect_ratio": "16:9",
  "image": "base64_encoded_image_string" // только для qwen-edit
}
```

**Ответ:**
```json
{
  "image": "base64_encoded_generated_image",
  "prompt": "a beautiful sunset over mountains",
  "model": "flux"
}
```

## Структура проекта

```
src/
├── components/          # React компоненты
│   ├── ui/             # UI компоненты (shadcn/ui)
│   └── GenerationForm.tsx # Основной компонент формы
├── hooks/              # React хуки
│   └── useTelegram.ts  # Интеграция с Telegram
├── store/              # Zustand store
│   └── generationStore.ts # Управление состоянием
└── App.tsx            # Главный компонент

api/
├── controllers/        # Контроллеры Express
│   └── generationController.ts # Логика генерации изображений
├── routes/            # Express роуты
│   └── generation.ts  # API роуты для генерации
└── app.ts             # Express приложение
```

## Деплой

### Railway (рекомендуется для Telegram Mini Apps)

#### Способ 1: Через Railway CLI

1. Установите Railway CLI:
```bash
npm install -g @railway/cli
```

2. Авторизуйтесь в Railway:
```bash
railway login
```

3. Создайте новый проект:
```bash
railway init
```

4. Добавьте переменные окружения:
```bash
railway variables set KIE_API_KEY=your_kie_ai_api_key_here
railway variables set NODE_ENV=production
```

5. Задеплойте проект:
```bash
railway up
```

#### Способ 2: Через веб-интерфейс Railway

1. Перейдите на [railway.app](https://railway.app)
2. Создайте новый проект и подключите GitHub репозиторий
3. Railway автоматически обнаружит `Dockerfile` и `railway.json`
4. Добавьте переменные окружения в настройках проекта:
   - `KIE_API_KEY` - ваш API ключ от Kie.ai
   - `NODE_ENV=production`
5. Нажмите "Deploy"

#### Способ 3: Через Procfile (альтернативный)

Если вы предпочитаете использовать Procfile вместо Docker, Railway автоматически обнаружит `Procfile` и задеплоит приложение как Node.js проект.

### Vercel (альтернативный вариант)

1. Подключите репозиторий к Vercel
2. Установите переменные окружения в настройках проекта
3. Нажмите "Deploy"

### Другие платформы

Приложение использует стандартный Node.js стек и может быть развернуто на любой платформе, поддерживающей Node.js.

## Безопасность

- API ключ Kie.ai хранится на сервере и не暴露给 клиенту
- Все запросы к AI API проходят через backend
- Поддержка CORS настроена для безопасной работы в Telegram

## Лицензия

MIT License

## Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте настройки переменных окружения
2. Убедитесь, что Kie.ai API ключ действителен
3. Проверьте консоль браузера на наличие ошибок
4. Убедитесь, что Telegram Bot правильно настроен для работы с Mini Apps
